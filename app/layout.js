export const metadata = {
  title: 'getItemsLru key-length repro',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
